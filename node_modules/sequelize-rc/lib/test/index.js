'use strict';

const db = require('../../index');
const path = require('path');
db.init({
    group: 'test',
    dataId: 'local',
    env: 'dev',
    password: '123123',
    database: 'wireless_print',
    username: 'root',
    port: '3306',
    host: '127.0.0.1',
    modelPath: path.resolve(__dirname, './models'),
});

setTimeout(function() {
    let User = db.getModel('user');
    User.findById(100001).then(user => {
        console.log(user)
    });
}, 1200);
